# Copie este arquivo para .env e configure suas variáveis

# URL da API (backend) - para rodar backend separado localmente
VITE_API_URL=http://localhost:3001
# Em produção (deploy unificado), frontend usa /api automaticamente (ver src/api.ts)

# Configurações do Supabase (caso futuramente use direto no frontend)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=sua_supabase_anon_key

# Ambiente (development, staging, production)
VITE_ENV=development