# Copie este arquivo para .env e configure suas variáveis

# Ambiente
NODE_ENV=development

# Porta do servidor local (não usada em serverless Vercel, apenas rodando localmente)
PORT=3001

# Autenticação JWT
JWT_SECRET=defina-uma-chave-secreta-segura-aqui

# Supabase (service key: use a service_role apenas no backend)
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_SERVICE_KEY=sua_supabase_service_key

# CORS (se rodar backend standalone)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Observação:
# Em produção na Vercel, defina apenas: JWT_SECRET, SUPABASE_URL, SUPABASE_SERVICE_KEY