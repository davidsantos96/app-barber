import styled from 'styled-components';

export const DisponibilidadeContainer = styled.div`
  max-width: 800px;
  margin: 2rem auto;
  background: #232526;
  border-radius: var(--border-radius);
  box-shadow: 0 2px 8px rgba(0,0,0,0.07);
  padding: 2rem;
  color: #fff;
`;

export const DisponibilidadeTitle = styled.h2`
  font-size: 1.7rem;
  margin-bottom: 1.2rem;
`;
